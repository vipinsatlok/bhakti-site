import React from "react";

const NotFoundData = () => {
  return (
    <div className="border animate-pulse font-bold rounded p-5 flex items-center justify-center">
        Not Found
    </div>
  );
};

export default NotFoundData;
